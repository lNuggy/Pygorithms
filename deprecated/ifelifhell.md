# If elif hell, from 23/3/23

```python
if args.algorithm == "bubble":
        sorted_arr = Algorithms.bubble_sort(arr)
    elif args.algorithm == "counting":
        sorted_arr = Algorithms.counting_sort(arr)
    elif args.algorithm == "shell":
        sorted_arr = Algorithms.shell_sort(arr)
    elif args.algorithm == "selection":
        sorted_arr = Algorithms.selection_sort(arr)
    elif args.algorithm == "insertion":
        sorted_arr = Algorithms.insertion_sort(arr)
    elif args.algorithm == "gnome":
        sorted_arr = Algorithms.gnome_sort(arr)
    elif args.algorithm == "merge":
        sorted_arr = Algorithms.merge_sort(arr)
    elif args.algorithm == "quick":
        sorted_arr = Algorithms.quick_sort(arr)
    elif args.algorithm == "heap":
        sorted_arr = Algorithms.heap_sort(arr)
    elif args.algorithm == "cocktail":
        sorted_arr = Algorithms.cocktail_sort(arr)
    elif args.algorithm == "comb":
        sorted_arr = Algorithms.comb_sort(arr)
    elif args.algorithm == "pigeonhole":
        sorted_arr = Algorithms.pigeonhole_sort(arr)
    elif args.algorithm == "stooge":
        sorted_arr = Algorithms.stooge_sort(arr)
```
